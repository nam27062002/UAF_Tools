<UserControl x:Class="DANCustomTools.Tools.Editor.Views.EditorMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:DANCustomTools.ViewModels"
             xmlns:views="clr-namespace:DANCustomTools.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar for SubTool switching -->
        <ToolBarTray Grid.Row="0" Background="LightGray">
            <ToolBar>
                <Button Content="Scene Explorer"
                        Command="{Binding SwitchToSceneExplorerCommand}"
                        ToolTip="Switch to Scene Explorer"/>
                <Separator/>
                <Button Content="Properties Editor"
                        Command="{Binding SwitchToPropertiesEditorCommand}"
                        ToolTip="Switch to Properties Editor"/>
                <Separator/>
                <!-- Status info -->
                <TextBlock Text="Active SubTool:" Margin="10,0,5,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding CurrentSubToolName}" FontWeight="Bold" VerticalAlignment="Center"/>
            </ToolBar>
        </ToolBarTray>

        <!-- Current SubTool Content Area -->
        <ContentControl Grid.Row="1"
                        Content="{Binding CurrentSubToolViewModel}"
                        Margin="5">
            <ContentControl.Resources>
                <!-- DataTemplate for SceneExplorerViewModel -->
                <DataTemplate DataType="{x:Type viewModels:SceneExplorerViewModel}">
                    <views:SceneExplorerView DataContext="{Binding}"/>
                </DataTemplate>

                <!-- DataTemplate for PropertiesEditorViewModel -->
                <DataTemplate DataType="{x:Type viewModels:PropertiesEditorViewModel}">
                    <views:PropertiesEditorView DataContext="{Binding}"/>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</UserControl>