<UserControl x:Class="DANCustomTools.Controls.ComponentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="300">

    <UserControl.Resources>
        <!-- Component item style -->
        <Style x:Key="ComponentItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2,1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MaterialDesignSelection}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource GameEditorAccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Component data template -->
        <DataTemplate x:Key="ComponentTemplate">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="Cog"
                                         Width="16" Height="16"
                                         VerticalAlignment="Center"
                                         Margin="0,0,8,0"/>
                <TextBlock Text="{Binding Name}"
                           VerticalAlignment="Center"
                           FontSize="11"/>
                <TextBlock Text="{Binding Type}"
                           VerticalAlignment="Center"
                           FontSize="10"
                           Opacity="0.7"
                           Margin="8,0,0,0"
                           FontStyle="Italic"/>
            </StackPanel>
        </DataTemplate>

        <!-- Context menu for components -->
        <ContextMenu x:Key="ComponentContextMenu">
            <MenuItem Header="Move Up"
                      Command="{Binding MoveUpCommand}"
                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="ArrowUp" Width="16" Height="16"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Move Down"
                      Command="{Binding MoveDownCommand}"
                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="ArrowDown" Width="16" Height="16"/>
                </MenuItem.Icon>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Cut"
                      Command="{Binding CutCommand}"
                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="ContentCut" Width="16" Height="16"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Copy"
                      Command="{Binding CopyCommand}"
                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="ContentCopy" Width="16" Height="16"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Paste"
                      Command="{Binding PasteCommand}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="ContentPaste" Width="16" Height="16"/>
                </MenuItem.Icon>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Delete"
                      Command="{Binding DeleteCommand}"
                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="Delete" Width="16" Height="16"/>
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with search -->
        <Border Grid.Row="0"
                Background="{StaticResource GameEditorAccentBrush}"
                Padding="8,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         x:Name="FilterTextBox"
                         materialDesign:HintAssist.Hint="Filter components..."
                         materialDesign:HintAssist.IsFloating="False"
                         FontSize="11"
                         Margin="0,0,8,0"
                         Text="{Binding FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Grid.Column="1"
                        Style="{StaticResource GameEditorToolButtonStyle}"
                        Command="{Binding ClearFilterCommand}"
                        ToolTip="Clear Filter"
                        Width="24" Height="24"
                        Padding="4">
                    <materialDesign:PackIcon Kind="Close" Width="12" Height="12"/>
                </Button>
            </Grid>
        </Border>

        <!-- Component lists -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Available Components -->
            <GroupBox Grid.Column="0"
                      Header="Available Components"
                      Margin="4,4,2,4">
                <ListBox ItemsSource="{Binding FilteredAvailableComponents}"
                         SelectedItem="{Binding SelectedAvailableComponent}"
                         ItemTemplate="{StaticResource ComponentTemplate}"
                         ItemContainerStyle="{StaticResource ComponentItemStyle}"
                         AllowDrop="True"
                         Drop="AvailableComponents_Drop"
                         DragOver="Components_DragOver">
                    <ListBox.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick"
                                      Command="{Binding AddComponentCommand}"
                                      CommandParameter="{Binding SelectedAvailableComponent}"/>
                    </ListBox.InputBindings>
                </ListBox>
            </GroupBox>

            <!-- Transfer buttons -->
            <StackPanel Grid.Column="1"
                        VerticalAlignment="Center"
                        Margin="4">
                <Button Command="{Binding AddComponentCommand}"
                        CommandParameter="{Binding SelectedAvailableComponent}"
                        Style="{StaticResource GameEditorToolButtonStyle}"
                        ToolTip="Add Component"
                        Margin="0,2">
                    <materialDesign:PackIcon Kind="ChevronRight" Width="20" Height="20"/>
                </Button>

                <Button Command="{Binding RemoveComponentCommand}"
                        CommandParameter="{Binding SelectedUsedComponent}"
                        Style="{StaticResource GameEditorToolButtonStyle}"
                        ToolTip="Remove Component"
                        Margin="0,2">
                    <materialDesign:PackIcon Kind="ChevronLeft" Width="20" Height="20"/>
                </Button>

                <Separator Margin="4,8"/>

                <Button Command="{Binding AddAllComponentsCommand}"
                        Style="{StaticResource GameEditorToolButtonStyle}"
                        ToolTip="Add All Components"
                        Margin="0,2">
                    <materialDesign:PackIcon Kind="ChevronDoubleRight" Width="20" Height="20"/>
                </Button>

                <Button Command="{Binding RemoveAllComponentsCommand}"
                        Style="{StaticResource GameEditorToolButtonStyle}"
                        ToolTip="Remove All Components"
                        Margin="0,2">
                    <materialDesign:PackIcon Kind="ChevronDoubleLeft" Width="20" Height="20"/>
                </Button>
            </StackPanel>

            <!-- Used Components -->
            <GroupBox Grid.Column="2"
                      Header="Actor Components"
                      Margin="2,4,4,4">
                <ListBox ItemsSource="{Binding UsedComponents}"
                         SelectedItem="{Binding SelectedUsedComponent}"
                         ItemTemplate="{StaticResource ComponentTemplate}"
                         ItemContainerStyle="{StaticResource ComponentItemStyle}"
                         AllowDrop="True"
                         Drop="UsedComponents_Drop"
                         DragOver="Components_DragOver"
                         ContextMenu="{StaticResource ComponentContextMenu}">
                    <ListBox.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick"
                                      Command="{Binding RemoveComponentCommand}"
                                      CommandParameter="{Binding SelectedUsedComponent}"/>
                    </ListBox.InputBindings>
                </ListBox>
            </GroupBox>
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="2"
                Background="{DynamicResource MaterialDesignDivider}"
                Padding="8,4">
            <DockPanel>
                <TextBlock Text="{Binding UsedComponents.Count, StringFormat=\{0\} components used}"
                           FontSize="10"
                           Opacity="0.8"
                           DockPanel.Dock="Left"/>

                <TextBlock Text="{Binding FilteredAvailableComponents.Count, StringFormat=\{0\} available}"
                           FontSize="10"
                           Opacity="0.8"
                           HorizontalAlignment="Right"
                           DockPanel.Dock="Right"/>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>