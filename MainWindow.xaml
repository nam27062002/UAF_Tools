<Window x:Class="DANCustomTools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DANCustomTools"
        xmlns:viewModels="clr-namespace:DANCustomTools.ViewModels"
        xmlns:views="clr-namespace:DANCustomTools.Views"
        mc:Ignorable="d"
        Title="UAF Tools" Height="600" Width="900">

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModels:SceneExplorerViewModel}">
            <views:SceneExplorerView DataContext="{Binding}"/>
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <StackPanel DockPanel.Dock="Left" Width="200" Background="#f0f0f0">
            <TextBlock Text="Tools" FontSize="16" Margin="10" FontWeight="Bold"/>
            <Button Content="Scene Explorer" Command="{Binding OpenSceneExplorerCommand}" Margin="10,0"/>
        </StackPanel>

        <ContentControl Content="{Binding CurrentViewModel}" />
    </DockPanel>
</Window>
