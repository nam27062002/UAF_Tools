<UserControl x:Class="DANCustomTools.Views.SceneExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:DANCustomTools.Converters"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:ObjectTypeConverter x:Key="ObjectTypeConverter"/>
        <converters:BoolToOnlineStatusConverter x:Key="BoolToOnlineStatusConverter"/>
        <converters:BoolToStatusColorConverter x:Key="BoolToStatusColorConverter"/>
        <converters:ConnectionStatusColorConverter x:Key="ConnectionStatusColorConverter"/>
        <converters:TreeItemIconConverter x:Key="TreeItemIconConverter"/>
        <converters:TreeItemTextFallbackConverter x:Key="TreeItemTextFallbackConverter"/>
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!-- Enhanced TreeView Item Style -->
        <Style x:Key="MaterialTreeViewItemStyle" TargetType="TreeViewItem">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <!-- Two-way bind selection & expansion state to ViewModel -->
            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Expander Button -->
                            <ToggleButton Grid.Column="0" Grid.Row="0"
                                          Name="Expander"
                                          ClickMode="Press"
                                          IsChecked="{Binding Path=IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                          Width="16" Height="16"
                                          Margin="0,0,4,0"
                                          VerticalAlignment="Center"
                                          Background="Transparent"
                                          BorderThickness="0">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border Background="Transparent" Width="20" Height="20">
                                                        <materialDesign:PackIcon Kind="ChevronRight"
                                                            Width="16" Height="16"
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"
                                                            Foreground="#666666"
                                                            RenderTransformOrigin="0.5,0.5">
                                                            <materialDesign:PackIcon.RenderTransform>
                                                                <RotateTransform x:Name="ExpanderRotateTransform" Angle="0"/>
                                                            </materialDesign:PackIcon.RenderTransform>
                                                        </materialDesign:PackIcon>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Trigger.EnterActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetName="ExpanderRotateTransform"
                                                                                         Storyboard.TargetProperty="Angle"
                                                                                         To="90" Duration="0:0:0.2"/>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </Trigger.EnterActions>
                                                            <Trigger.ExitActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetName="ExpanderRotateTransform"
                                                                                         Storyboard.TargetProperty="Angle"
                                                                                         To="0" Duration="0:0:0.2"/>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </Trigger.ExitActions>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>

                            <!-- Content -->
                            <Border Grid.Column="1" Grid.Row="0"
                                    Name="ContentBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="4"
                                    Margin="0,1">
                                <ContentPresenter Name="PART_Header"
                                                  Content="{TemplateBinding Header}"
                                                  ContentTemplate="{TemplateBinding HeaderTemplate}"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"/>
                            </Border>

                            <!-- Children -->
                            <ItemsPresenter Grid.Column="1" Grid.Row="1"
                                            Name="ItemsHost"
                                            Margin="20,0,0,0"/>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Hide item if IsVisible is False -->
                            <DataTrigger Binding="{Binding IsVisible}" Value="False">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <Trigger Property="IsExpanded" Value="False">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                            <!-- Enhanced Selection Highlight -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ContentBorder" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#2196F3" Offset="0"/>
                                            <GradientStop Color="#64B5F6" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="ContentBorder" Property="BorderBrush" Value="#1976D2"/>
                                <Setter TargetName="ContentBorder" Property="BorderThickness" Value="2"/>
                                <Setter TargetName="PART_Header" Property="TextElement.Foreground" Value="White"/>
                                <Setter TargetName="PART_Header" Property="TextElement.FontWeight" Value="SemiBold"/>
                                <Setter TargetName="ContentBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#2196F3" BlurRadius="8" ShadowDepth="0" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ContentBorder" Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="ContentBorder" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#1976D2" Offset="0"/>
                                            <GradientStop Color="#42A5F5" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="ContentBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#1976D2" BlurRadius="12" ShadowDepth="0" Opacity="0.8"/>
                                    </Setter.Value>
                                </Setter>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Search TextBox Style -->
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource GameEditorSearchBoxStyle}">
            <Setter Property="Margin" Value="12"/>
        </Style>
    </UserControl.Resources>

    <materialDesign:Card Style="{StaticResource GameEditorCardStyle}" Margin="0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" MinWidth="250" MaxWidth="400"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header with Connection Status -->
            <Border Grid.Row="0" Grid.ColumnSpan="3"
                    Background="{StaticResource GameEditorHeaderBrush}"
                    Padding="16,12">
                <DockPanel>
                    <materialDesign:PackIcon Kind="FileTree"
                                             Width="22" Height="22"
                                             VerticalAlignment="Center"
                                             Foreground="White"
                                             DockPanel.Dock="Left"/>
                    <TextBlock Text="Scene Explorer"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="White"
                               VerticalAlignment="Center"
                               Margin="12,0,0,0"
                               DockPanel.Dock="Left"/>

                    <!-- Connection Status -->
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                DockPanel.Dock="Right">
                        <Ellipse Width="8" Height="8"
                                 Margin="0,0,6,0"
                                 VerticalAlignment="Center">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{StaticResource GameEditorWarningBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                            <Setter Property="Fill" Value="{StaticResource GameEditorSuccessBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Text="{Binding IsConnected, Converter={StaticResource BoolToOnlineStatusConverter}}"
                                   FontSize="11"
                                   FontWeight="Medium"
                                   Foreground="White"
                                   VerticalAlignment="Center"
                                   Opacity="0.9"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- Search Box + Action Buttons Combined -->
            <Border Grid.Row="1" Grid.ColumnSpan="3"
                    Background="{DynamicResource MaterialDesignBackground}"
                    BorderThickness="0,1,0,1"
                    BorderBrush="{DynamicResource MaterialDesignDivider}"
                    Padding="8,3">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Box -->
                    <TextBox Grid.Column="0"
                             Name="SearchTextBox"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             TextChanged="SearchTextBox_TextChanged"
                             Margin="0,0,8,0"
                             Height="32"
                             FontSize="12"
                             FontFamily="Segoe UI"
                             Foreground="Black"
                             Background="White"
                             BorderBrush="#CCCCCC"
                             BorderThickness="1"
                             Padding="8,0"
                             VerticalContentAlignment="Center"
                             HorizontalContentAlignment="Left"
                             materialDesign:HintAssist.Hint="Search actors and scenes..."
                             materialDesign:HintAssist.Foreground="#999999">
                        <TextBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </TextBox.Resources>
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Name="border"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="4"
                                                    SnapsToDevicePixels="True">
                                                <ScrollViewer x:Name="PART_ContentHost"
                                                              Focusable="false"
                                                              HorizontalScrollBarVisibility="Hidden"
                                                              VerticalScrollBarVisibility="Hidden"
                                                              Margin="{TemplateBinding Padding}"
                                                              VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsEnabled" Value="false">
                                                    <Setter Property="Opacity" TargetName="border" Value="0.56"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="true">
                                                    <Setter Property="BorderBrush" TargetName="border" Value="#999999"/>
                                                </Trigger>
                                                <Trigger Property="IsKeyboardFocused" Value="true">
                                                    <Setter Property="BorderBrush" TargetName="border" Value="#2196F3"/>
                                                    <Setter Property="BorderThickness" TargetName="border" Value="2"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <!-- Action Buttons -->
                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal">
                        <Button Style="{StaticResource GameEditorToolButtonStyle}"
                                ToolTip="Refresh Scene Tree"
                                Command="{Binding RefreshCommand}"
                                Margin="0,0,3,0"
                                Padding="6,3"
                                Height="32">
                            <materialDesign:PackIcon Kind="Refresh" Width="14" Height="14"/>
                        </Button>

                        <Button Style="{StaticResource GameEditorToolButtonStyle}"
                                ToolTip="Expand All"
                                Click="ExpandAllButton_Click"
                                Margin="0,0,3,0"
                                Padding="6,3"
                                Height="32">
                            <materialDesign:PackIcon Kind="UnfoldMoreHorizontal" Width="14" Height="14"/>
                        </Button>

                        <Button Style="{StaticResource GameEditorToolButtonStyle}"
                                ToolTip="Collapse All"
                                Click="CollapseAllButton_Click"
                                Margin="0,0,3,0"
                                Padding="6,3"
                                Height="32">
                            <materialDesign:PackIcon Kind="UnfoldLessHorizontal" Width="14" Height="14"/>
                        </Button>

                        <!-- Auto-Update Toggle Button -->
                        <Button Command="{Binding ToggleAutoUpdateCommand}"
                                Margin="0,0,8,0"
                                Padding="6,3"
                                Height="32"
                                ToolTip="Toggle Auto-Update (disable for large scenes)">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#F5F5F5"/>
                                    <Setter Property="Foreground" Value="#333333"/>
                                    <Setter Property="BorderBrush" Value="#CCCCCC"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="FontSize" Value="11"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="4"
                                                        Padding="{TemplateBinding Padding}">
                                                    <StackPanel Orientation="Horizontal">
                                                        <materialDesign:PackIcon Kind="AutoFix" 
                                                                                 Width="14" Height="14"
                                                                                 VerticalAlignment="Center"
                                                                                 Margin="0,0,4,0"/>
                                                        <TextBlock Text="{Binding AutoUpdateEnabled, StringFormat='Auto-Update: {0}', Converter={StaticResource BoolToOnlineStatusConverter}}"
                                                                   VerticalAlignment="Center"
                                                                   TextBlock.Foreground="{TemplateBinding Foreground}"/>
                                                    </StackPanel>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AutoUpdateEnabled}" Value="True">
                                            <Setter Property="Background" Value="#4CAF50"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="BorderBrush" Value="#388E3C"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="#999999"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <!-- Object Type Filter Buttons -->
                        <Button Command="{Binding ShowAllObjectsCommand}"
                                Margin="0,0,2,0"
                                Padding="8,3"
                                Height="32"
                                MinWidth="45"
                                ToolTip="Show All Objects">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#F5F5F5"/>
                                    <Setter Property="Foreground" Value="#333333"/>
                                    <Setter Property="BorderBrush" Value="#CCCCCC"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="FontSize" Value="11"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="4"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"
                                                                      TextBlock.Foreground="{TemplateBinding Foreground}"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsShowingAll}" Value="True">
                                            <Setter Property="Background" Value="#2196F3"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="BorderBrush" Value="#1976D2"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="#999999"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="All"/>
                        </Button>

                        <Button Command="{Binding ShowActorsOnlyCommand}"
                                Margin="0,0,2,0"
                                Padding="8,3"
                                Height="32"
                                MinWidth="55"
                                ToolTip="Show Actors Only">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#F5F5F5"/>
                                    <Setter Property="Foreground" Value="#333333"/>
                                    <Setter Property="BorderBrush" Value="#CCCCCC"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="FontSize" Value="11"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="4"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"
                                                                      TextBlock.Foreground="{TemplateBinding Foreground}"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsShowingActorsOnly}" Value="True">
                                            <Setter Property="Background" Value="#4CAF50"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="BorderBrush" Value="#388E3C"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="#999999"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="Actors"/>
                        </Button>

                        <Button Command="{Binding ShowFrisesOnlyCommand}"
                                Padding="8,3"
                                Height="32"
                                MinWidth="50"
                                ToolTip="Show Frises Only">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#F5F5F5"/>
                                    <Setter Property="Foreground" Value="#333333"/>
                                    <Setter Property="BorderBrush" Value="#CCCCCC"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="FontSize" Value="11"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="4"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"
                                                                      TextBlock.Foreground="{TemplateBinding Foreground}"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsShowingFrisesOnly}" Value="True">
                                            <Setter Property="Background" Value="#FF9800"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="BorderBrush" Value="#F57C00"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="#999999"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="Frises"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Component Filter Section -->
            <!-- Left Panel: Component Filters Section -->
            <Border Grid.Row="2" Grid.Column="0"
                    Margin="12,0,0,8"
                    Background="{DynamicResource MaterialDesignBackground}"
                    CornerRadius="4"
                    BorderThickness="1"
                    BorderBrush="{DynamicResource MaterialDesignDivider}">
                <Expander Header="Component Filters"
                          IsExpanded="True"
                          Padding="8,4">
                    <Expander.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Filter"
                                                         Width="16" Height="16"
                                                         VerticalAlignment="Center"
                                                         Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding}"
                                           VerticalAlignment="Center"
                                           FontWeight="Medium"/>
                                <TextBlock Text="{Binding DataContext.SelectedComponentsCount, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat='({0} selected)'}"
                                           VerticalAlignment="Center"
                                           Margin="6,0,0,0"
                                           FontSize="11"
                                           Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                            </StackPanel>
                        </DataTemplate>
                    </Expander.HeaderTemplate>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Component Search Box -->
                        <TextBox Grid.Row="0"
                                 Text="{Binding ComponentSearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,0,8"
                                 Height="28"
                                 FontSize="11"
                                 Padding="8,4"
                                 Background="{DynamicResource MaterialDesignBackground}"
                                 BorderBrush="{DynamicResource MaterialDesignDivider}"
                                 BorderThickness="1"
                                 VerticalContentAlignment="Center">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="TextBox">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="4">
                                                    <Grid>
                                                        <ScrollViewer x:Name="PART_ContentHost"
                                                                     Focusable="False"
                                                                     HorizontalScrollBarVisibility="Hidden"
                                                                     VerticalScrollBarVisibility="Hidden"/>
                                                        <TextBlock Text="Search components..."
                                                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                                   VerticalAlignment="Center"
                                                                   Margin="8,0"
                                                                   IsHitTestVisible="False">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </TextBox.Style>
                        </TextBox>

                        <!-- Filter Controls -->
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,4">
                            <Button Content="Select All"
                                    Command="{Binding ClearFiltersCommand}"
                                    Padding="6,2"
                                    FontSize="10"
                                    Background="Transparent"
                                    BorderBrush="{DynamicResource MaterialDesignDivider}"
                                    BorderThickness="1"
                                    Height="24"
                                    Margin="0,0,4,0">
                                <Button.IsEnabled>
                                    <Binding Path="IsComponentFilterEnabled"/>
                                </Button.IsEnabled>
                            </Button>

                            <Button Content="Unselect All"
                                    Command="{Binding UnselectAllFiltersCommand}"
                                    Padding="6,2"
                                    FontSize="10"
                                    Background="Transparent"
                                    BorderBrush="{DynamicResource MaterialDesignDivider}"
                                    BorderThickness="1"
                                    Height="24"
                                    Margin="0,0,8,0">
                                <Button.IsEnabled>
                                    <Binding Path="HasComponents"/>
                                </Button.IsEnabled>
                            </Button>

                            <TextBlock Text="{Binding FilteredComponents.Count, StringFormat={}{0} components shown}"
                                       VerticalAlignment="Center"
                                       Margin="0,0,0,0"
                                       FontSize="10"
                                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        </StackPanel>

                        <!-- Component List -->
                        <ScrollViewer Grid.Row="3"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      Padding="2">
                            <ItemsControl ItemsSource="{Binding FilteredComponents}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Vertical" Margin="2"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ToggleButton Content="{Binding DisplayText}"
                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                      Margin="2"
                                                      Padding="8,3"
                                                      FontSize="10"
                                                      FontWeight="Medium"
                                                      MinWidth="200"
                                                      MinHeight="24"
                                                      HorizontalAlignment="Stretch">
                                            <ToggleButton.Style>
                                                <Style TargetType="ToggleButton">
                                                    <Setter Property="Background" Value="#F5F5F5"/>
                                                    <Setter Property="Foreground" Value="#333333"/>
                                                    <Setter Property="BorderBrush" Value="#CCCCCC"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                    <Setter Property="Cursor" Value="Hand"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="ToggleButton">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                        CornerRadius="4"
                                                                        Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Left"
                                                                                      VerticalAlignment="Center"
                                                                                      TextBlock.Foreground="{TemplateBinding Foreground}"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter Property="Background" Value="#2196F3"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                            <Setter Property="BorderBrush" Value="#1976D2"/>
                                                        </Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="BorderBrush" Value="#999999"/>
                                                        </Trigger>
                                                        <MultiTrigger>
                                                            <MultiTrigger.Conditions>
                                                                <Condition Property="IsMouseOver" Value="True"/>
                                                                <Condition Property="IsChecked" Value="True"/>
                                                            </MultiTrigger.Conditions>
                                                            <Setter Property="Background" Value="#1976D2"/>
                                                        </MultiTrigger>
                                                        <Trigger Property="IsChecked" Value="False">
                                                            <Setter Property="Background" Value="#FFEBEE"/>
                                                            <Setter Property="Foreground" Value="#D32F2F"/>
                                                            <Setter Property="BorderBrush" Value="#FFCDD2"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ToggleButton.Style>
                                        </ToggleButton>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Expander>
            </Border>

            <!-- Grid Splitter -->
            <GridSplitter Grid.Row="2" Grid.Column="1"
                          Width="5"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Background="{DynamicResource MaterialDesignDivider}"
                          ShowsPreview="True"/>

            <!-- Right Panel: Main Tree View -->
            <Border Grid.Row="2" Grid.Column="2"
                    Margin="0,0,12,8"
                    Background="{DynamicResource MaterialDesignBackground}"
                    CornerRadius="6"
                    BorderThickness="1"
                    BorderBrush="{DynamicResource MaterialDesignDivider}">
                <Grid>
                    <!-- Empty State -->
                    <StackPanel VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                Visibility="{Binding SceneTreeItems.Count, Converter={StaticResource CountToVisibilityConverter}}">
                        <materialDesign:PackIcon Kind="FileTreeOutline"
                                                 Width="64" Height="64"
                                                 Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                 HorizontalAlignment="Center"/>
                        <TextBlock Text="No scene data available"
                                   FontSize="16"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,4"/>
                        <TextBlock Text="Connect to engine to load scene hierarchy"
                                   FontSize="12"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                   HorizontalAlignment="Center"
                                   TextAlignment="Center"/>
                    </StackPanel>

                    <!-- Tree View -->
                    <TreeView Name="SceneTreeView"
                              ItemsSource="{Binding SceneTreeItems}"
                              Background="Transparent"
                              BorderThickness="0"
                              Padding="8"
                              VirtualizingStackPanel.IsVirtualizing="False"
                              ScrollViewer.CanContentScroll="False"
                              SelectedItemChanged="TreeView_SelectedItemChanged"
                              MouseRightButtonUp="TreeView_MouseRightButtonUp"
                              KeyDown="TreeView_KeyDown"
                              ItemContainerStyle="{StaticResource MaterialTreeViewItemStyle}">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <StackPanel Orientation="Horizontal"
                                            Margin="2,0">
                                    <!-- Item Icon -->
                                    <Image Source="{Binding ItemType, Converter={StaticResource TreeItemIconConverter}}"
                                           Width="18" Height="18"
                                           Margin="0,0,8,0"
                                           VerticalAlignment="Center"
                                           Stretch="Uniform"
                                           RenderOptions.BitmapScalingMode="HighQuality"/>

                                    <!-- Item Text -->
                                    <TextBlock Text="{Binding DisplayName}"
                                               VerticalAlignment="Center"
                                               FontSize="13"
                                               FontWeight="Normal"
                                               Foreground="#333333"/>
                                </StackPanel>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </Grid>
            </Border>

            <!-- Status Bar -->
            <Border Grid.Row="3" Grid.ColumnSpan="3"
                    Background="{StaticResource GameEditorBorderBrush}"
                    Padding="16,8">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ViewList"
                                             Width="14" Height="14"
                                             VerticalAlignment="Center"
                                             Foreground="{StaticResource GameEditorTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding SceneTreeItems.Count, StringFormat='Scenes: {0}'}"
                               FontSize="11"
                               FontWeight="Medium"
                               Foreground="{StaticResource GameEditorTextSecondaryBrush}"
                               Margin="6,0,20,0"
                               VerticalAlignment="Center"/>

                    <!-- Component Filter Status -->
                    <materialDesign:PackIcon Kind="Filter"
                                             Width="14" Height="14"
                                             VerticalAlignment="Center"
                                             Foreground="{StaticResource GameEditorTextSecondaryBrush}"
                                             Visibility="{Binding IsComponentFilterEnabled, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    <TextBlock Text="{Binding SelectedComponents.Count, StringFormat='Filters: {0}'}"
                               FontSize="11"
                               FontWeight="Medium"
                               Foreground="{StaticResource GameEditorTextSecondaryBrush}"
                               Margin="6,0,20,0"
                               VerticalAlignment="Center"
                               Visibility="{Binding IsComponentFilterEnabled, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                    <!-- Auto-Update Status -->
                    <materialDesign:PackIcon Kind="AutoFix"
                                             Width="14" Height="14"
                                             VerticalAlignment="Center"
                                             Foreground="{StaticResource GameEditorTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding AutoUpdateEnabled, StringFormat='Auto-Update: {0}', Converter={StaticResource BoolToOnlineStatusConverter}}"
                               FontSize="11"
                               FontWeight="Medium"
                               Foreground="{StaticResource GameEditorTextSecondaryBrush}"
                               Margin="6,0,20,0"
                               VerticalAlignment="Center"/>

                    <materialDesign:PackIcon Kind="CursorDefault"
                                             Width="14" Height="14"
                                             VerticalAlignment="Center"
                                             Foreground="{StaticResource GameEditorTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding SelectedObject.FriendlyName, StringFormat='Selected: {0}', FallbackValue='No selection'}"
                               FontSize="11"
                               FontWeight="Medium"
                               Foreground="{StaticResource GameEditorTextSecondaryBrush}"
                               Margin="6,0,0,0"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </materialDesign:Card>
</UserControl>
