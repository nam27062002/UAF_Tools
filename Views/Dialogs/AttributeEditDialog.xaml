<Window x:Class="DANCustomTools.Views.Dialogs.AttributeEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Modify Attribute"
        Width="450" Height="200"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource GameEditorBackgroundBrush}"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="DialogButtonStyle" TargetType="Button" BasedOn="{StaticResource GameEditorToolButtonStyle}">
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="4"/>
        </Style>
    </Window.Resources>

    <materialDesign:Card Style="{StaticResource GameEditorCardStyle}" Margin="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="{StaticResource GameEditorAccentBrush}"
                    Padding="16,12"
                    CornerRadius="4,4,0,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="EditAttributes"
                                             Width="24" Height="24"
                                             VerticalAlignment="Center"
                                             Foreground="White"/>
                    <TextBlock Text="Edit Attribute Properties"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="White"
                               VerticalAlignment="Center"
                               Margin="12,0,0,0"/>
                </StackPanel>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Attribute Name -->
                <Label Grid.Row="0"
                       Content="Attribute Name:"
                       FontWeight="SemiBold"
                       Margin="0,0,0,4"/>

                <TextBox Grid.Row="1"
                         x:Name="AttributeTextBox"
                         Text="{Binding AttributeName, Mode=TwoWay}"
                         IsReadOnly="True"
                         Background="{DynamicResource MaterialDesignDivider}"
                         Padding="8"
                         Margin="0,0,0,16"
                         FontFamily="Consolas, Courier New, monospace"/>

                <!-- Description -->
                <Label Grid.Row="2"
                       Content="Description:"
                       FontWeight="SemiBold"
                       Margin="0,0,0,4"/>

                <TextBox Grid.Row="3"
                         x:Name="DescriptionTextBox"
                         Text="{Binding Description, Mode=TwoWay}"
                         AcceptsReturn="False"
                         TextWrapping="Wrap"
                         Padding="8"
                         MinHeight="40"
                         VerticalContentAlignment="Top"
                         materialDesign:HintAssist.Hint="Enter attribute description..."
                         materialDesign:HintAssist.IsFloating="False"/>
            </Grid>

            <!-- Button Panel -->
            <Border Grid.Row="2"
                    Background="{DynamicResource MaterialDesignDivider}"
                    Padding="16,12">
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Style="{StaticResource DialogButtonStyle}"
                            Content="Cancel"
                            IsCancel="True"
                            Click="CancelButton_Click">
                        <Button.ToolTip>
                            <ToolTip Content="Cancel changes and close dialog"/>
                        </Button.ToolTip>
                    </Button>

                    <Button Style="{StaticResource DialogButtonStyle}"
                            Content="OK"
                            IsDefault="True"
                            Click="OkButton_Click">
                        <Button.ToolTip>
                            <ToolTip Content="Apply changes and close dialog"/>
                        </Button.ToolTip>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </materialDesign:Card>
</Window>