<UserControl x:Class="DANCustomTools.Views.PropertiesEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:DANCustomTools.Converters"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:tc="clr-namespace:TechnoControls.XMLPropertyGrid;assembly=TechnoControls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="400">

    <UserControl.Resources>
        <!-- Boolean to Visibility Converter -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        
        <!-- Bool to Online Status Converter -->
        <converters:BoolToOnlineStatusConverter x:Key="BoolToOnlineStatusConverter"/>

        <!-- Inverted Boolean to Visibility Converter -->
        <Style x:Key="InvertedVisibilityStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasData}" Value="True">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource GameEditorSurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource GameEditorBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource GameEditorHoverBrush}"/>
                                <Setter Property="BorderBrush" Value="{StaticResource GameEditorAccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource GameEditorSelectedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#f8f9fa"/>
                                <Setter Property="Foreground" Value="#6c757d"/>
                                <Setter Property="BorderBrush" Value="#e9ecef"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="XmlTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#dee2e6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="FontFamily" Value="Consolas,Monaco,monospace"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="AcceptsTab" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
        </Style>

        <!-- Indentation converter for hierarchical rows -->
        <converters:IndentLevelToWidthConverter x:Key="IndentConverter"/>
    </UserControl.Resources>

    <materialDesign:Card Style="{StaticResource GameEditorCardStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="{StaticResource GameEditorHeaderBrush}"
                    Padding="16,12">
                <DockPanel>
                    <materialDesign:PackIcon Kind="TuneVertical"
                                             Width="22" Height="22"
                                             VerticalAlignment="Center"
                                             Foreground="White"
                                             DockPanel.Dock="Left"/>
                    <TextBlock Text="Properties Editor"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="White"
                               VerticalAlignment="Center"
                               Margin="12,0,0,0"
                               DockPanel.Dock="Left"/>

                    <!-- Connection Status -->
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                DockPanel.Dock="Right">
                        <Ellipse Width="8" Height="8"
                                 Margin="0,0,6,0"
                                 VerticalAlignment="Center">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{StaticResource GameEditorWarningBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                            <Setter Property="Fill" Value="{StaticResource GameEditorSuccessBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Text="{Binding IsConnected, Converter={StaticResource BoolToOnlineStatusConverter}}"
                                   FontSize="11"
                                   FontWeight="Medium"
                                   Foreground="White"
                                   VerticalAlignment="Center"
                                   Opacity="0.9"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- Content -->
            <Border Grid.Row="1" Background="White" BorderBrush="#ddd" BorderThickness="1" Margin="4">
            <Grid>
                <!-- Empty State -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                           Style="{StaticResource InvertedVisibilityStyle}">
                    <TextBlock Text="🎯" FontSize="32" HorizontalAlignment="Center" Foreground="#ccc" Margin="0,0,0,8"/>
                    <TextBlock Text="Select an object in the Scene Tree to view its properties"
                              FontSize="12"
                              Foreground="#666"
                              HorizontalAlignment="Center"
                              TextWrapping="Wrap"
                              TextAlignment="Center"/>
                </StackPanel>

                <!-- Properties Content: host original WinForms XMLPropertyGrid for pixel-perfect parity -->
                <TabControl Visibility="{Binding HasData, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Margin="0">
                    <TabItem Header="Properties">
                        <Grid x:Name="WinFormsHostContainer" />
                    </TabItem>
                    <TabItem Header="XML">
                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Auto">
                            <TextBox Text="{Binding XmlDisplayText, UpdateSourceTrigger=PropertyChanged}"
                                    Style="{StaticResource XmlTextBoxStyle}"
                                    MinHeight="300"/>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>
            </Grid>
        </Border>
        </Grid>
    </materialDesign:Card>
</UserControl>
