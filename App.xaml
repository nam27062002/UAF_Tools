<Application x:Class="DANCustomTools.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DANCustomTools"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Material Design Theme -->
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="Blue" SecondaryColor="Teal" />

                <!-- Custom Resources -->
                <ResourceDictionary>
                    <!-- Modern Color Palette -->
                    <SolidColorBrush x:Key="GameEditorPrimaryBrush" Color="#1E3A8A"/>      <!-- Deep Blue -->
                    <SolidColorBrush x:Key="GameEditorAccentBrush" Color="#059669"/>       <!-- Emerald Green -->
                    <SolidColorBrush x:Key="GameEditorSuccessBrush" Color="#10B981"/>      <!-- Success Green -->
                    <SolidColorBrush x:Key="GameEditorWarningBrush" Color="#F59E0B"/>      <!-- Amber -->
                    <SolidColorBrush x:Key="GameEditorErrorBrush" Color="#EF4444"/>        <!-- Red -->
                    <SolidColorBrush x:Key="GameEditorBackgroundBrush" Color="#F8FAFC"/>   <!-- Slate 50 -->
                    <SolidColorBrush x:Key="GameEditorSurfaceBrush" Color="#FFFFFF"/>      <!-- Pure White -->
                    <SolidColorBrush x:Key="GameEditorTextBrush" Color="#1E293B"/>         <!-- Slate 800 -->
                    <SolidColorBrush x:Key="GameEditorTextSecondaryBrush" Color="#64748B"/> <!-- Slate 500 -->
                    <SolidColorBrush x:Key="GameEditorBorderBrush" Color="#E2E8F0"/>       <!-- Slate 200 -->
                    <SolidColorBrush x:Key="GameEditorHoverBrush" Color="#F1F5F9"/>        <!-- Slate 100 -->
                    <SolidColorBrush x:Key="GameEditorSelectedBrush" Color="#DBEAFE"/>     <!-- Blue 100 -->
                    <SolidColorBrush x:Key="GameEditorHeaderBrush" Color="#0F172A"/>       <!-- Slate 900 -->

                    <!-- Enhanced Custom Styles -->
                    <Style x:Key="GameEditorCardStyle" TargetType="materialDesign:Card">
                        <Setter Property="Background" Value="{StaticResource GameEditorSurfaceBrush}"/>
                        <Setter Property="Margin" Value="8"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="BorderBrush" Value="{StaticResource GameEditorBorderBrush}"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.15" Color="#1E293B"/>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style x:Key="GameEditorButtonStyle" TargetType="Button">
                        <Setter Property="Background" Value="{StaticResource GameEditorPrimaryBrush}"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Height" Value="36"/>
                        <Setter Property="Padding" Value="16,8"/>
                        <Setter Property="FontWeight" Value="Medium"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="8"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#1D4ED8"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="#1E40AF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect BlurRadius="6" ShadowDepth="2" Opacity="0.2" Color="#1E3A8A"/>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style x:Key="GameEditorToolButtonStyle" TargetType="Button">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource GameEditorTextSecondaryBrush}"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Width" Value="36"/>
                        <Setter Property="Height" Value="36"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource GameEditorHoverBrush}"/>
                                            <Setter Property="Foreground" Value="{StaticResource GameEditorTextBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="{StaticResource GameEditorAccentBrush}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <!-- Panel Header Style -->
                    <Style x:Key="GameEditorPanelHeaderStyle" TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource GameEditorHeaderBrush}"/>
                        <Setter Property="Padding" Value="16,12"/>
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1" Color="#000000"/>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <!-- Search Box Style -->
                    <Style x:Key="GameEditorSearchBoxStyle" TargetType="TextBox">
                        <Setter Property="Background" Value="{StaticResource GameEditorSurfaceBrush}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource GameEditorBorderBrush}"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Padding" Value="12,8"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TextBox">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="6">
                                        <ScrollViewer x:Name="PART_ContentHost" 
                                                      Margin="{TemplateBinding Padding}"
                                                      VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter Property="BorderBrush" Value="{StaticResource GameEditorAccentBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
